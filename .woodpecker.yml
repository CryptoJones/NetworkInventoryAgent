steps:
  build:
    image: golang:1.25
    commands:
      - go build ./...

  vet:
    image: golang:1.25
    commands:
      - go vet ./...

  fmt:
    image: golang:1.25
    commands:
      - test -z "$(gofmt -l .)"

  test:
    image: golang:1.25
    commands:
      - go test -race -timeout 120s ./...

  vuln:
    image: golang:1.25
    commands:
      - go install golang.org/x/vuln/cmd/govulncheck@latest
      - govulncheck ./...

  docker:
    image: plugins/docker
    settings:
      repo: networkinventoryagent
      dry_run: true
    when:
      event: push
